import React from "react";
import moment from "moment";
const Notification = props => {
  const { notifications } = props;
  return <div className="section">Notification</div>;
};

export default Notification;
